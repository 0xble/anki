# Changelog

## Fork Convention (0xble)

- Upstream entries are implicit by default (no prefix required).
- Prefix fork-authored divergences with `[FORK]`.
- Prefix uncommitted/local-only work with `[LOCAL]`.

## [Unreleased]

### Changed

- [LOCAL] Initialize fork changelog convention tracking for this fork (uncommitted).
- [FORK] Normalize the `-0xble` fork suffix to a PEP 440 local version for Python wheel builds.
